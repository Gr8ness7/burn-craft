[project]
name = "burn-craft"
authors = ["web3-dev"]
description = "A programmable token burning mechanism with advanced supply reduction strategies"
telemetry = false
requirements = []
[contracts.burn-token]
path = "contracts/burn-token.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
